language: php
dist: xenial
sudo: false

php:
  - 5.6
  - 7.0
  - 7.1

services:
  - mongodb

addons:
  apt:
    packages:
      - php-mongodb

before_install:
  - sudo add-apt-repository ppa:ondrej/php -y
  - sudo apt-get update
  - sudo apt-get install -y --force-yes php-mongodb
  - echo "extension = mongodb.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
  - phpenv config-rm xdebug.ini
  - composer --verbose self-update --stable
  - composer --version

install:
  - composer --verbose install

script:
  - ./vendor/bin/phpunit
